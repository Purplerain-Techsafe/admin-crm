import { InternLeads } from '@/components/InternLeads';
import { DashboardLayout } from '@/components/DashboardLayout';

export default function InternLeadsPage() {
    return (
        <DashboardLayout activePage="intern-leads">
            <InternLeads />
        </DashboardLayout>
    );
}
